# M5 PR10 — Identity Provider Adapter

Provider-neutral identity provider adapter for token validation and user lookup.

Keycloak, Azure AD, Google, GitHub, Apple, OIDC, SAML and LDAP must be implemented as provider-specific adapters.
