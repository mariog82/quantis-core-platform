# Reporting Runtime Guide

## Purpose

Reporting Runtime generates report outputs through provider-neutral renderers.

## Components

- `ReportDefinition`
- `ReportContext`
- `ReportOutput`
- `ReportRegistry`
- `ReportRenderer`
- `ReportingRuntime`

## Default renderer

The default runtime includes a text renderer for tests and development.
