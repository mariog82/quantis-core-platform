# M3 Public API Baseline

The following package entry points are public for the M3 beta:

```python
modules.analytics
modules.reporting
modules.dashboard
modules.notification
modules.registry
```

## Compatibility Rule

Within M3 beta, exported symbols should not be removed without a compatibility note.
