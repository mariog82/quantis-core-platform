# Design Notes

The first BPMN layer supports a controlled BPMN subset:

- start events;
- end events;
- service tasks;
- user tasks;
- script tasks;
- timer catch events;
- exclusive and parallel gateways;
- sequence flows.

External BPMN engines are not required.
