# M7 WP1 PR3 — Relationship Engine

Version: `0.7.0-alpha.3`

Introduces typed, directed, undirected, weighted and semantic relationships,
relationship indexes, graph traversal and shortest-path search.

This PR also introduces the official version command:

```powershell
python -m quantis version
```
